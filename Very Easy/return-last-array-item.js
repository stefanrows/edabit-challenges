// Create a function that accepts an array and returns the last item in the array.

const items = [ 'Duck', 'Elephant', 'Witcher' ];

const getLastItem = (arr) => {
	return arr.pop();
};

getLastItem(items);
